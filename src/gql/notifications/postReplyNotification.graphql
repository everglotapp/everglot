query PostReplyNotification($id: Int!) {
    post(id: $id) {
        authorId
        parentPost {
            authorId
            snowflakeId
        }
        body
        createdAt
        author {
            displayName
            username
        }
        snowflakeId
    }
}
