query PostLikeNotification($id: Int!) {
    postLike(id: $id) {
        post {
            authorId
            body
            parentPostId
            snowflakeId
            uuid
        }
        user {
            id
            username
            uuid
            displayName
        }
    }
}
