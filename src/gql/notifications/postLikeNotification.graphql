query PostLikeNotification($id: Int!) {
    postLike(id: $id) {
        post {
            authorId
            body
            parentPostId
            snowflakeId
        }
        user {
            id
            username
            displayName
        }
    }
}
