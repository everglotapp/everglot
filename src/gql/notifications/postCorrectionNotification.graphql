query PostCorrectionNotification($id: Int!) {
    postCorrection(id: $id) {
        post {
            authorId
            body
            parentPostId
            snowflakeId
        }
        user {
            id
            username
            displayName
        }
    }
}
