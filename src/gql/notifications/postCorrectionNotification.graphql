query PostCorrectionNotification($id: Int!) {
    postCorrection(id: $id) {
        post {
            authorId
            body
            parentPostId
            snowflakeId
            uuid
        }
        user {
            id
            username
            uuid
            displayName
        }
    }
}
