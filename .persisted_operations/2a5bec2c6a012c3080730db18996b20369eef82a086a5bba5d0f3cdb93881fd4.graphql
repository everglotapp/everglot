query PostUserMentionNotification($id: Int!) {
  postUserMention(id: $id) {
    userId
    postId
    post {
      parentPost {
        authorId
        snowflakeId
        __typename
      }
      body
      author {
        displayName
        username
        __typename
      }
      __typename
    }
    __typename
  }
}