query PostGameByUuid($uuid: UUID!) {
    postGameByUuid(uuid: $uuid) {
        id
        nodeId
        ranges {
            nodes {
                uuid
                id
                caseOption
                genderOption
                startIndex
                endIndex
            }
        }
        gameType
        post {
            authorId
        }
    }
}
