query UserFollowershipNotification($id: Int!) {
    userFollower(id: $id) {
        follower {
            uuid
            username
            displayName
        }
        user {
            id
        }
    }
}
