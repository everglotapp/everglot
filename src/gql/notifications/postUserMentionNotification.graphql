query PostUserMentionNotification($id: Int!) {
    postUserMention(id: $id) {
        userId
        postId
        post {
            parentPost {
                authorId
                snowflakeId
            }
            body
            author {
                displayName
                username
            }
        }
    }
}
