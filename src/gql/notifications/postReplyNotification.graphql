query PostReplyNotification($id: Int!) {
    post(id: $id) {
        authorId
        parentPost {
            authorId
        }
        body
        createdAt
        author {
            displayName
            username
        }
    }
}
