query PostUserMentionNotification($id: Int!) {
    postUserMention(id: $id) {
        userId
        user {
            uuid
        }
        postId
        post {
            author {
                displayName
                username
                uuid
            }
            body
            parentPost {
                authorId
                snowflakeId
            }
            uuid
        }
    }
}
